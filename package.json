{
  "name": "f2link",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init-db": "wrangler d1 execute f2link --file=./schema.sql",
    "set-webhook": "wrangler deploy && curl -F \"url=$(wrangler deploy --dry-run --outdir=dist 2>&1 | grep -oP 'https://[^ ]+' | head -1)/webhook\" https://api.telegram.org/bot$BOT_TOKEN/setWebhook"
  },
  "dependencies": {
    "telegraf": "^4.19.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
